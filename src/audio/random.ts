// Noise and offsets for the synthesis – nothing here is a secret, so the pseudorandom generator is fine.
// eslint-disable-next-line sonarjs/pseudo-random
export const random = (): number => Math.random();
